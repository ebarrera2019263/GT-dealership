# URL pública del API (incluí el prefijo /api). Se inyecta en build: hay que
# tenerla seteada ANTES de compilar la web.
# Local:  http://localhost:3001/api
# Prod:   https://<tu-api>.up.railway.app/api
NEXT_PUBLIC_API_URL="http://localhost:3001/api"
