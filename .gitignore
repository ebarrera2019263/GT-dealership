# Dependencias
node_modules/

# Builds
dist/
.next/
out/
*.tsbuildinfo

# Entorno — los secretos nunca van al repo
.env
.env.local
.env.*.local

# Prisma
apps/api/prisma/generated/

# Imágenes subidas (contenido de usuario, no va al repo)
apps/api/uploads/

# Logs y sistema
*.log
.DS_Store

# Cobertura de tests
coverage/

# Graphify: se versionan graph.json, GRAPH_REPORT.md y graph.html; los
# archivos internos de trabajo no
graphify-out/.graphify_*
graphify-out/cache/

# Artefactos de pruebas de navegador (solo capturas sueltas en la raíz)
.playwright-mcp/
/*.png

# Claude Code — settings personales (no compartir)
.claude/settings.local.json
